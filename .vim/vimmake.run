#! /bin/sh
# guake -s 0 -e "fg;ls;ls;ls;ls;ls"

# if [ $TEST ];then
#     echo $TEST
#     echo "...."
#     export TEST=${TEST}+1
# else
#     export TEST=1
# fi
# guake --show  -e "cd ${VIM_TARGET};make run;cd -;"

cd $VIM_TARGET
make run
cd -
